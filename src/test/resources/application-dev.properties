# Test-only overrides for the dev profile so `mvn clean install` does not depend on IDE run configuration env vars.
# These values are intentionally non-secret and only need to satisfy property resolution during context startup.

jwt-secret=VGhpcy1pcy1hLXRlc3Qtc2VjcmV0LWtleS0zMi1ieXRlcw==
jwt-expiration-ms=86400000

spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=test@example.com
spring.mail.password=test-password
spring.mail.from=test@example.com

cloudinary.cloud_name=test-cloud
cloudinary.api_key=test-api-key
cloudinary.api_secret=test-api-secret

site.base.url=http://localhost:8080

