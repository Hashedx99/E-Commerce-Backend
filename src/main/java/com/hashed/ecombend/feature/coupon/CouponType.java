package com.hashed.ecombend.feature.coupon;

public enum CouponType {
    /**
     * Reduces the order by a fixed dollar amount.
     */
    FIXED_AMOUNT,
    /**
     * Reduces the order by a percentage of the subtotal.
     */
    PERCENTAGE
}
